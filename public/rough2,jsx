import React, { useState } from 'react';
import { Link } from 'react-router-dom';

/* ================= DATA SOURCE ================= */
const LIGHT_TOPICS = [
  { id: 1, title: "Light Mixing", desc: "Additive and subtractive color theory.", path: "/light/mixing" },
  { id: 2, title: "Color Pigment Mixing", desc: "Understanding pigment interaction.", path: "/light/pigment" },
  { id: 3, title: "Polarization of Light", desc: "Visualize light wave orientation and filters.", path: "/light/polarization" },
  { id: 4, title: "Double Slit Diffraction and Interference", desc: "The classic experiment explained.", path: "/light/double-slit-diffraction" },
  { id: 5, title: "Double Slit Interference", desc: "Wave superposition in two-slit setups.", path: "/light/double-slit" },
  { id: 6, title: "Diffraction Grating Laser Lab", desc: "Analyze laser interference patterns.", path: "/light/diffraction-grating" },
  { id: 7, title: "Thin Film Interference", desc: "Colors in soap bubbles and oil films.", path: "/light/thin-film" },
  { id: 8, title: "Reflection and Refraction", desc: "Snell's Law and surface interaction.", path: "/light/reflection-refraction" },
  { id: 9, title: "Dispersion of Light", desc: "Prisms and the visible spectrum.", path: "/light/dispersion" },
  { id: 10, title: "Plane Mirrors", desc: "Virtual image formation basics.", path: "/light/plane-mirrors" },
  { id: 11, title: "Concave and Convex Mirrors", desc: "Ray tracing with curved mirrors.", path: "/light/curved-mirrors" },
  { id: 12, title: "Lenses & Chromatic Aberration", desc: "Lens properties and color distortion.", path: "/light/lenses-aberration" },
  { id: 13, title: "2D Image Formation by Lenses", desc: "Ray diagrams for image construction.", path: "/light/2d-lenses" },
  { id: 14, title: "Optics of the Human Eye", desc: "How light focuses on the retina.", path: "/light/human-eye" },
  { id: 15, title: "Rainbow Formation", desc: "Internal reflection and refraction in droplets.", path: "/light/rainbow" }
];

/* ================= MAIN COMPONENT ================= */
export default function LightMenu() {
  return (
    <div style={styles.wrapper}>
      <h1 style={styles.header}>Light & Optics Simulations</h1>
      
      <div style={styles.grid}>
        {LIGHT_TOPICS.map((topic) => (
          <MenuCard key={topic.id} topic={topic} />
        ))}
      </div>
    </div>
  );
}

/* ================= CARD COMPONENT ================= */
function MenuCard({ topic }) {
  const [hover, setHover] = useState(false);
  // Matches your requested naming convention: light1.png, light2.png, etc.
  const imagePath = `/assets/light/light${topic.id}.png`;

  return (
    <Link to={topic.path} style={{ textDecoration: 'none' }}>
      <div 
        style={{
          ...styles.card,
          borderColor: hover ? "rgba(0, 168, 255, 0.4)" : "#222",
          transform: hover ? "scale(1.03)" : "scale(1)",
          boxShadow: hover ? "0 10px 30px rgba(0, 168, 255, 0.15)" : "0 4px 10px rgba(0,0,0,0.3)"
        }}
        onMouseEnter={() => setHover(true)}
        onMouseLeave={() => setHover(false)}
      >
        <div style={styles.preview}>
          <div style={styles.gridPattern} />
          <img 
            src={imagePath} 
            alt={topic.title}
            style={{
              ...styles.image,
              opacity: hover ? 1 : 0.8,
              transform: hover ? "scale(1.05)" : "scale(1)"
            }}
            onError={(e) => {
              e.target.style.display = 'none';
              e.target.parentNode.style.background = '#111';
            }}
          />
        </div>
        <div style={styles.content}>
          <h3 style={styles.title}>{topic.title}</h3>
          <p style={styles.desc}>{topic.desc}</p>
        </div>
      </div>
    </Link>
  );
}

/* ================= STYLES ================= */
const styles = {
  wrapper: {
    minHeight: '100vh',
    background: '#050505',
    color: '#fff',
    padding: '60px 40px',
    fontFamily: 'Inter, system-ui, sans-serif',
  },
  header: {
    fontSize: '24px',
    fontWeight: '400',
    letterSpacing: '1px',
    marginBottom: '40px',
    borderBottom: '1px solid #222',
    paddingBottom: '20px',
    color: '#eee'
  },
  grid: {
    display: 'grid',
    gridTemplateColumns: 'repeat(auto-fill, minmax(300px, 1fr))',
    gap: '24px',
  },
  card: {
    background: '#0a0a0c',
    border: '1px solid #222',
    borderRadius: '12px',
    overflow: 'hidden',
    transition: 'all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1)',
    cursor: 'pointer',
    display: 'flex',
    flexDirection: 'column',
    height: '100%',
  },
  preview: {
    height: '180px',
    position: 'relative',
    borderBottom: '1px solid #1a1a1a',
    overflow: 'hidden',
    background: '#08080a',
  },
  image: {
    width: '100%',
    height: '100%',
    objectFit: 'cover',
    transition: 'all 0.5s ease',
    display: 'block',
  },
  gridPattern: {
    position: 'absolute',
    top: 0, left: 0, right: 0, bottom: 0,
    backgroundImage: `
      linear-gradient(to right, rgba(255,255,255,0.03) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(255,255,255,0.03) 1px, transparent 1px)
    `,
    backgroundSize: '20px 20px',
    pointerEvents: 'none',
    zIndex: 1,
  },
  content: {
    padding: '20px',
    flex: 1,
    display: 'flex',
    flexDirection: 'column',
  },
  title: {
    fontSize: '16px',
    fontWeight: '600',
    margin: '0 0 8px 0',
    color: '#fff',
    lineHeight: '1.4',
  },
  desc: {
    fontSize: '13px',
    color: '#888',
    margin: 0,
    lineHeight: '1.5',
  },
};